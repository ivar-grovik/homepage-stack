
services:

  homepage:

    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    ports:
      - 3001:3000
    volumes:
      - /home/pi/dockers/homepage:/app/config # Make sure your local config directory exists
      - /var/run/docker.sock:/var/run/docker.sock # (optional) For docker integrations
    environment:
      HOMEPAGE_ALLOWED_HOSTS: gethomepage.dev,192.168.68.50:3000,100.109.108.52:3000 # required, may need port. See gethomepage.dev/installation/#homepage_allowed_hosts
    restart: unless-stopped
    depends_on:
      - line2-proxy

  line2-proxy:
    build: ./line2-proxy
    container_name: line2-proxy
    ports:
      - 3000:3000
    restart: unless-stopped
